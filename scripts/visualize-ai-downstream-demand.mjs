import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const page = path.join(root, 'dist', 'projects', 'ai-downstream-demand', 'index.html');
if (!fs.existsSync(page)) throw new Error(`Missing monitor page: ${page}`);

let html = fs.readFileSync(page, 'utf8');

if (!html.includes('.comparison-chart{')) {
  html = html.replace('</style>', `.comparison-chart{margin-top:18px}.comparison-group{border-top:1px solid var(--rule);padding:14px 0}.comparison-group:first-child{border-top:0}.comparison-title{font-size:.78rem;font-weight:700;margin-bottom:10px}.comparison-row{display:grid;grid-template-columns:150px 1fr 48px;gap:10px;align-items:center;margin:8px 0}.comparison-row span{font-size:.72rem;color:var(--muted)}.comparison-track{height:16px;background:#edf0f2;position:relative}.comparison-bar{height:100%;background:var(--accent)}.comparison-bar-secondary{opacity:.58}.comparison-row strong{text-align:right;font-family:Georgia,serif;font-size:1rem}.comparison-scale{display:flex;justify-content:space-between;font-size:.66rem;color:var(--muted);margin:6px 0 0 160px}.payment-bar-row{display:grid;grid-template-columns:105px 1fr 64px 92px;gap:10px;align-items:center;border-top:1px solid var(--rule);padding:13px 0}.payment-bar-row:first-child{border-top:0}.payment-name{font-weight:700;font-size:.82rem}.payment-track{height:18px;background:#edf0f2}.payment-bar{height:100%;background:var(--accent)}.payment-value{text-align:right;font-family:Georgia,serif;font-size:1.05rem}.payment-change{text-align:right;font-size:.7rem;color:var(--up);font-weight:700}.payment-scale{display:flex;justify-content:space-between;margin:4px 156px 0 115px;font-size:.66rem;color:var(--muted)}.intensity-bars{margin-top:14px}.intensity-row{display:grid;grid-template-columns:54px 1fr 72px;gap:9px;align-items:center;margin:8px 0}.intensity-row span{font-size:.72rem;color:var(--muted)}.intensity-track{height:15px;background:#edf0f2}.intensity-bar{height:100%;background:var(--accent)}.intensity-row strong{text-align:right;font-family:Georgia,serif;font-size:.92rem}@media(max-width:650px){.comparison-row{grid-template-columns:112px 1fr 44px}.comparison-scale{margin-left:122px}.payment-bar-row{grid-template-columns:82px 1fr 56px}.payment-change{grid-column:2/4;text-align:left}.payment-scale{margin-left:92px;margin-right:66px}}` + '</style>');
}

if (!html.includes('data-payment-visual="true"')) {
  const script = `<script data-payment-visual="true">(async()=>{try{const r=await fetch('./payment.json');const d=await r.json();const vendors=document.getElementById('payment-vendors');if(vendors){vendors.innerHTML=d.vendors.map(v=>'<div class="payment-bar-row"><div class="payment-name">'+v.name+'</div><div class="payment-track" role="img" aria-label="'+v.name+' paid adoption '+v.value.toFixed(1)+' percent"><div class="payment-bar" style="width:'+Math.min(100,v.value/50*100)+'%"></div></div><div class="payment-value">'+v.value.toFixed(1)+'%</div><div class="payment-change">↑ '+v.monthlyChangePp.toFixed(2)+' pp MoM</div></div>').join('')+'<div class="payment-scale"><span>0%</span><span>50%</span></div>';}const box=document.querySelector('.intensity-box');if(box&&!document.getElementById('intensity-bars')){const max=Math.max(d.intensity.julyPerEmployeeUsd,d.intensity.augustPerEmployeeUsd);const bars=document.createElement('div');bars.id='intensity-bars';bars.className='intensity-bars';bars.innerHTML='<div class="intensity-row"><span>July</span><div class="intensity-track"><div class="intensity-bar" style="width:'+(d.intensity.julyPerEmployeeUsd/max*100)+'%"></div></div><strong>$'+d.intensity.julyPerEmployeeUsd.toLocaleString()+'</strong></div><div class="intensity-row"><span>August</span><div class="intensity-track"><div class="intensity-bar" style="width:'+(d.intensity.augustPerEmployeeUsd/max*100)+'%"></div></div><strong>$'+d.intensity.augustPerEmployeeUsd.toLocaleString()+'</strong></div>';const big=document.getElementById('intensity-value');if(big)big.style.display='none';box.insertBefore(bars,document.getElementById('intensity-change'));}}catch(e){/* existing textual fallback remains */}})();</script>`;
  html = html.replace('</body>', script + '</body>');
}

fs.writeFileSync(page, html);
console.log('Added graphical deployment and payment views');
